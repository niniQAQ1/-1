def move_smallest_to_end(number_list): 
    smallest_value = min(number_list)
    last_index = len(number_list) - 1 - number_list[::-1].index(smallest_value)
    if last_index != len(number_list) - 1:
        number_list[last_index], number_list[-1] = number_list[-1], number_list[last_index]
